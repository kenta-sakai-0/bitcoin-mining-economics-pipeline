# bitcoin-mining-ecomomics-pipeline

## Setups

### GCP Auth

GCP Authentication uses Application Default Credentials. Download service account JSON and modify filepath under `docker-compose.yaml`and`dagster.yaml`.
